/// Web-only: update document title + Open Graph meta when a shop catalog loads.
/// No-op on mobile/desktop.
void updateStorefrontSeo({
  required String title,
  required String description,
  String? imageUrl,
  required String pageUrl,
}) {}
